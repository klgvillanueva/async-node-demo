/**
 * @name constants
 * @description contains constant data reused across approaches
 */

const path = require('path');

module.exports = {
  // fs constants:
  BLOG_PATH: path.join(__dirname, '../blog-posts/my-first-feature-article.md'),
  DB_URL: 'my-mock-db',
};
