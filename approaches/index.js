const callbacks = require('./callbacks');
const callbacksRefactored = require('./callbacks-refactored');
const promises = require('./promises');
const promiseAll = require('./promise-all');
const asyncAwait = require('./async-await');
const asyncAwaitRefactored = require('./async-await-refactored');

module.exports = {
  callbacks,
  callbacksRefactored,
  promises,
  promiseAll,
  asyncAwait,
  asyncAwaitRefactored,
};
